/**
 * Experimental features kept in source for later iteration.
 *
 * Custom template creation, import/export and selection remain fully
 * implemented, but are intentionally hidden from the current UI.
 * See docs/CUSTOM_TEMPLATES.md before enabling.
 */
export const SHOW_CUSTOM_TEMPLATES = false;
